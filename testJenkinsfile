pipeline {
    agent any  // Use any available Jenkins agent

    stages {
        stage('Install Dependencies') {
            agent { 
                docker { 
                    image 'node:14'  // Use the official Node.js Docker image
                } 
            }
            steps {
                sh 'npm install'  // Run npm install to install project dependencies
            }
        }
    }
}

